Welcome, <%= current_user.username %>!

Awards You've Created:

<% current_user.awards.each do |award| %>
  <p><%= award.title %></p>
<% end %>

<%= link_to "Log Out", logout_path, method: :delete %>
